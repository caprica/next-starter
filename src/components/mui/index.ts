/**
 * MUI does not (yet) support React Server Components so we use this compatibility layer to use MUI
 * components as client components.
 */

'use client'

import { Button } from '@mui/material'

export { Button }
