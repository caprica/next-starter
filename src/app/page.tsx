import { Button } from '@/components/mui'

export default function Home() {
    return (
        <main>
            <h1>Ready to go...</h1>
            <Button variant='contained' color='primary'>
                I am button
            </Button>
        </main>
    )
}
